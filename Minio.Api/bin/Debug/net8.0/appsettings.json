{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Minio": {
    "Endpoint": "localhost:9000", // Alamat server MinIO
    "AccessKey": "admin", // Username MinIO
    "SecretKey": "admin12345", // Password MinIO
    "BucketName": "bucket01", // Nama bucket yang sudah dibuat
    "WithSSL": false // SSL aktif/tidak
  }
}
