{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Minio": {
    "Endpoint": "localhost:9000", // Alamat server MinIO
    "AccessKey": "xxx", // Username MinIO
    "SecretKey": "xxx", // Password MinIO
    "BucketName": "xxx", // Nama bucket yang sudah dibuat
    "WithSSL": false // SSL aktif/tidak
  }
}
